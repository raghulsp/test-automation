# Platform: web | android | ios
platform=android

# Browser: chrome | firefox  (used when platform=web)
browser=chrome
headless=false

# Base URL for web tests
base.url=https://the-internet.herokuapp.com

# Timeouts (seconds)
implicit.wait=10

# Appium (used when platform=android or ios)
appium.url=http://127.0.0.1:4725
device.name=ZA222ZPJC7
platform.version=16
app.package= com.netflix.mediaclient
app.activity=com.netflix.mediaclient.acquisition.screens.signupContainer.SignupNativeActivity
bundle.id=com.example.app
no.reset=false
