# Platform: web | android | ios
platform=

# Browser: chrome | firefox  (used when platform=web)
browser=chrome
headless=false

# Base URL for web tests
base.url=https://www.amazon.in

# Timeouts (seconds)
implicit.wait=10

# Appium (used when platform=android or ios)
appium.url=http://127.0.0.1:4723
device.name=ZA222ZPJC7
platform.version=16
app.package=in.amazon.mShop.android.shopping
app.activity=com.amazon.mShop.navigation.MainActivity
bundle.id=com.example.app
no.reset=false
