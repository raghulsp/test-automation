# QA environment — backed by public stub APIs
api.base.url.user=https://reqres.in/api
api.base.url.auth=https://reqres.in/api
api.base.url.product=https://fakestoreapi.com
api.base.url.order=https://jsonplaceholder.typicode.com

api.connection.timeout=10000
api.socket.timeout=30000

# Credentials used by TokenManager to obtain the shared bearer token once per JVM run
api.auth.email=eve.holt@reqres.in
api.auth.password=cityslicka
# TTL fallback (seconds) when the login response has no "expires_in" field
api.auth.token.ttl.seconds=3600
# Proactive refresh window — token is refreshed this many seconds before expiry
api.auth.token.buffer.seconds=30
